import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter.ifrn.edu.br.tsi.espelho',
  appName: 'Espelho',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
